# Student-Database-Management-System-SQL-
# 🏫 Student Database Management System (SQL)

A simple SQL project to manage student records using a relational database.  
This project includes table creation, data insertion, and common operations like updating, deleting, filtering, and sorting student data.

## 🚀 Features
- Create and manage student records  
- Insert, update, and delete operations  
- Query students by marks or department  
- Aggregate data (average marks, total students)

## 🧠 Concepts Used
CREATE, INSERT, SELECT, UPDATE, DELETE, WHERE, ORDER BY, GROUP BY, COUNT, AVG

## 🗂️ Database Used
MySQL / SQLite / PostgreSQL (works on any)

## 📄 Description (250 chars)
A beginner SQL project to manage student details such as name, department, marks, and grade. It supports basic CRUD operations, filtering by conditions, and computing aggregates like average marks and total students.
